# AA Prototype — PROGRESS

The running record of the build. **Every phase session reads this file first and updates it last.**
It holds the binding conventions, the status of every phase, the decisions log, and a per-phase
entry describing what was *actually* built (which may differ from the plan — record the truth).

## Status

| # | Phase | Status | Session date | Notes |
|---|-------|--------|--------------|-------|
| 00 | Scaffold & app shell | DONE | 2026-07-22 | Vite+React18+TS+Tailwind v4; tokens, shell, phone frame, 3 apps + 3 demo stubs |
| 01 | Domain model & billing calculator | DONE | 2026-07-22 | Full typed model, pure advanceable clock, NHI both formats, NZHIS subset + lookup, BTM calculator + validator; 116 tests green |
| 02 | Seed data & store | DONE | 2026-07-23 | Deterministic seed (design-day fidelity, 23 scenario markers), one Zustand store + audit wrapper + lifecycle guards, live clock rolls the canvas, `/demo/data` inspector; 188 tests green |
| 03 | Mobile: schedule & cards | NOT STARTED | — | — |
| 04 | Mobile: BTM capture & submission | NOT STARTED | — | — |
| 05 | Anaesthetist web app | NOT STARTED | — | — |
| 06 | Admin: day dashboard & changes | NOT STARTED | — | — |
| 07 | Admin: authorisation & masters | NOT STARTED | — | — |
| 08 | Billing: run & invoices | NOT STARTED | — | — |
| 09 | Billing: exceptions & monitor | NOT STARTED | — | — |
| 10 | Xero & payments simulation | NOT STARTED | — | — |
| 11 | Integrations simulation | NOT STARTED | — | — |
| 12 | Demo polish & guided script | NOT STARTED | — | — |

Statuses: NOT STARTED → IN PROGRESS → DONE (with date). If a phase is partially done at the end
of a session, leave it IN PROGRESS and state exactly what remains.

## Binding conventions (all phases)

1. **App location.** The app lives in `aa-prototype/` at the repo root. Docs live in `docs/prototype-build/`. Never create a second app folder.
2. **Stack is fixed.** Vite + React 18 + TypeScript (strict, no `any`), React Router, Zustand for the store, Tailwind CSS + a design-token layer (`src/theme/`), lucide-react icons, date-fns, Vitest for unit tests. No additional UI framework without a decisions-log entry.
3. **Folder shape.** `src/domain/` (types, seed, billing maths, validators — pure, no React), `src/store/` (Zustand slices, lifecycle guards, audit writer), `src/apps/mobile/`, `src/apps/web/`, `src/apps/admin/`, `src/apps/demo/` (demo-only surfaces: control panel, Xero sim, integrations, billing monitor), `src/shared/` (cross-app components), `src/shell/` (app switcher, phone frame, layout).
4. **Fake backend only.** Components never own domain state; they read/write through typed store hooks. No `fetch`, no real endpoints. Simulated external systems (Xero, HL7, PDF/OCR) are store actions triggered from the demo control panel or demo screens.
5. **Deterministic seed.** All seed data generated from a seeded RNG + pinned demo date (`DEMO_TODAY`, 2026-07-21 — the design mockups' content date, see Decisions log). Same seed → identical data. Reset restores the pristine seed. Never call `Date.now()`/`new Date()` for domain logic — use the demo clock.
6. **Lifecycle rules live in the store.** DRAFT → SUBMITTED → AUTHORISED transitions are guarded functions; SUBMITTED strips anaesthetist edit rights; AUTHORISED locks Cards immutable; there is **no Returned state**. UI can never bypass a guard.
7. **Audit is append-only.** Every Card/Procedure mutation (and List reassignment/state change) writes an audit entry `{who, role, source, action, before→after, at}`. No mutation without one.
8. **NHI never crosses to Xero.** Xero-sim screens and data show only the hidden internal ID (ContactNumber) and ContactID. This is checked in Phase 10's tests.
9. **Billing maths is pure and tested.** All fee/unit/route/split logic lives in `src/domain/billing/` as pure functions with Vitest tests. UI only formats results.
10. **RFP vocabulary, RFP colours.** Domain terms exactly as the RFP uses them. List-status colour legend is defined once in `src/theme/statusColours.ts` (modernised palette, 1:1 with the legacy legend: private, public, pre-op assessment, holiday, unavailable, free) and used by all three apps.
11. **Ease of use wins arguments.** When choosing between fidelity to the legacy screenshot and a simpler modern pattern, choose simpler — but keep the information architecture recognisable.
12. **Phone frame is the only mobile emulation.** The mobile app renders in the fixed phone frame component on a grey backdrop; never rely on browser viewport size. Web/admin apps are desktop layouts.
13. **Demo-only surfaces are visibly demo-only.** Xero sim, integration simulator, billing monitor internals and the control panel carry a "demo simulation" badge so demo audiences don't mistake them for proposed product UI. (The billing monitor itself IS proposed product UI — badge only its simulation triggers.)
14. **Every phase ends the same way:** run `npm run build` + `npx vitest run` green, run the phase's manual test checklist, update this file (status row + phase entry + any decisions), and leave the app runnable with `npm run dev`.
15. **Don't gold-plate ahead.** Build only what the current phase's doc lists; later phases depend on the seams described in their docs, not on speculative extras.
16. **Mobile is mobile-first, not a web app in a phone shell.** Inside the phone frame: bottom sheets and slide-in cards, never centred desktop modals; bottom tab bar (Lists · Availability · Balances · More); thumb-reachable and sticky contextual actions; steppers/chips/segmented controls over dropdowns and free text. Micro-delight lives in action feedback (complete-tick, fee value ticking up, submit settling) and never delays a task. The two web apps are proper desktop layouts (hover states, keyboard-friendly tables).
17. **The `docs/design/` mockups are the authoritative visual reference.** `Design Language.dc.html` is the token source of truth — palette, neutrals, the six status colours (with tint/on-tint values and the hatched Unavailable / dashed Free treatments), type (Schibsted Grotesk UI + Spline Sans Mono data with tabular-nums), 4pt spacing, radii (ctl 10 / card 14 / panel 20 / sheet 24-top / pill 999), elevations e-0…e-3, scrim rgba(23,35,32,0.32), and the four named motion patterns (sheet-in 320/260ms, card-advance 260/240ms with −24% parallax, complete-tick, value-tick; reduced-motion → 80ms fades). The six sample pages are the layout reference for their screens; each phase doc names its pages. Two hard rules: (a) AA crimson #A91E3E is identity only — masthead, active nav, avatars — never buttons, never status; deep teal #0D6E63 is the only action colour. (b) **Where a mockup simplified a business rule for demo purposes, the RFP rule wins** — see the Decisions log entries below. Extend the design's patterns to screens it doesn't cover; don't invent a second visual language.

## Decisions log

Record every deviation from a phase doc, every resolved ambiguity, and every killed idea, with a
one-line why. Later sessions must not re-litigate entries here.

- **2026-07-21 · Design run adopted as visual starting point** (user decision). Outputs in `docs/design/`; convention 17 added. Entries below capture its rulings.
- **2026-07-21 · DEMO_TODAY = 2026-07-21** (was 2026-06-23). The mockups' content lives on Tue 21 Jul 2026; the seed follows the design so screens match the reference 1:1.
- **2026-07-21 · Demo personas from the design**: anaesthetist persona = **Dr Melanie Souter** ("MS", crimson-tint avatar), office persona = **Kirsty W.** Seed must include them and the mockups' supporting cast (Rutherford, Sharma, Ngata, Beaumont, A. Chen, Ropata, Delaney, Fitzgerald, Hughes, Morrison, Whitaker, Ngatai, Strand — 14 total).
- **2026-07-21 · Status colour mapping** (from Design Language): Private #2E66E5, Public #6E56CF, Pre-op #C26A0E, Holiday #D25C74, Unavailable #64716C (hatched fill), Free #1FA463 (dashed border, "inviting"). Never colour-only — every block carries its label.
- **2026-07-21 · Mockup simplifications the build must NOT inherit** (RFP rules win, design layout stays): (a) time units shown as flat "15 min each" — the build uses the tiered T1/T2 rule (1u/15min first 2h, 1u/10min after); (b) modifier chips shown as a 3-chip demo set (Age>70 +1 / After hours +2 / Emergency +3) — the build uses all the RFP-named modifier groups (PA, A, AS, ASE, OB, P1, AI1, post-op) rendered in the same chip pattern, with unit values taken from the RFP's example ranges and explicitly labelled as demo-plausible, not an authoritative NZSA schedule (discovery item — see 2026-07-22 entry); (c) "ASA ≥ 3 adds +1" — the build maps AS1–AS4 to their real unit values via ASA seeding M; (d) Admin Review's "Route" column showed anaesthetic technique (GA/Spinal) — the build's Route column shows the RFP billing route (Hospital / Billable Party / Insurer); technique can be a secondary detail.
- **2026-07-21 · Navigation structures from the design**: web app top-nav = Dashboard · Lists · Availability · **Accounts** (Accounts houses Overdue + GST summary); admin = dark-ink side nav (Day view · Review queue with count badge · …) — the design shows a subset (Anaesthetists/Accounts/Reports); the build extends the same pattern with Billing monitor, Master data and Audit items.
- **2026-07-21 · New interactions the design added, adopted into scope**: mobile request-cover flow (tap a free session → bottom sheet → send cover request → tick confirmation; simulated notification), Everyone/Free-only segmented filters on availability (mobile + web), web "Ask to cover" links, admin review authorised-state choreography (green tick banner, table dims, per-row lock icons, queue badge decrements, "Next in queue →").
- **2026-07-21 · Phone frame**: adapt `docs/design/ios-frame.jsx` (IOSDevice — bezel, status bar, home indicator, no deps) into `src/shell/PhoneFrame.tsx` rather than building one from scratch; grey backdrop behind it: #E4E8E6.
- **2026-07-21 · Fonts**: Schibsted Grotesk + Spline Sans Mono via Google Fonts (matches mockups; internet assumed for the demo).
- **2026-07-22 · External plan review (Codex) — adopted rulings.** 12 findings accepted and folded into REQUIREMENTS + phase docs: (1) list submission requires every Card *marked Completed*, not merely valid — completion is validation-gated, submission is completion-gated; (2) anaesthetist availability writes to the AnaesthetistAvailability *master* and is reconciled into the canvas (free sessions restatus; booked sessions conflict-flag), never written to Lists directly; (3) reassignment slot mechanics: target session must be Free, its empty List is absorbed, the vacated slot regenerates (default Unavailable) — canvas invariant (2 Lists/anaesthetist/day) is guard-tested; (4) the mandatory default Type 1 contract per hospital/direct-insurer is a protected store invariant (cannot delete/end-date); (5) Contract-Holder route resolves by holder — hospital, surgeon or group (bariatric/TMJ/COS) — and the surgeon-held seed contract must be exercised by a billing run; (6) Xero contacts split into persistent organisational contacts (never archived, no hidden ID) vs patient contacts (hidden-ID workflow); (7) intake checks demoed: NHI dedupe across episodes, outstanding-balance surfacing before a new episode bills, archived-contact invoice/unarchive-TBC handling; (8) pre-payment typed full|split with an advisory unpaid-before-procedure warning (no hard block); (9) apps read the billing engine's mirror only — never the Xero-sim slice (greppable: view selectors import from `billing`, not `xero`); (10) per-hospital HL7 field-mapping config view, load-bearing in the failure-fix flow; (11) HPI identifiers on anaesthetists + FHIR Practitioner resources; (12) integration idempotency/dedupe by message control ID + dead-letter framing, demoed by duplicate replay.
- **2026-07-22 · Review findings NOT treated as defects:** the "60% vs 80%" permanent-list figure was a denominator confusion — the RFP's ~80% is the share of *surgeon assignments* from Permanent Lists, not of all sessions; seed inventory now states it in the RFP's terms. Invoice "printing" is satisfied by browser print of the on-screen document (no PDF library — scope fence stands); nib presentation gets an upload-portal status stub. The NHI-in-Xero choice stays implemented per Appendix 2 but is explicitly surfaced in the Xero-sim UI as an RFP contradiction requiring an AA ruling in discovery — it is a flagged reading, not a settled requirement.
- **2026-07-22 · Second external plan review (Codex) — adopted rulings, all 11 real.** **Supersedes item (8) of the prior entry**: pre-payment is now a REAL gate (completeCard blocks on an unpaid selfFundedPrepayment card), not an advisory warning — lifted only by an audited `overridePrepaymentGate(cardId, reason)`, visibly flagged wherever the card appears. New rulings: (1) index.html's stale "writes straight onto the canvas" line for availability corrected to match convention 2/D5 (reconciliation, not direct write) — a documentation-sync miss, not a new design decision; (2) Anaesthetist master gains registration-number-as-ID and contact details (was implicit, now explicit) alongside unitValue/GST period/HPI; (3) `reassignCard(cardId, toListId)` added as a distinct, lighter-weight guard from `reassignList` — moves one Card between Lists without touching either List's other Cards/status, audited at Card level (the RFP's routine case; List reassignment is the illness-cover case); (4) Contract/ContractPrice matching made explicit: `scope` (organisation | individualAnaesthetist reserved) and explicit matching keys (contractId + rvgBaseCode + surgeonId + procedureOrdinal); (5) `patientPaymentCategory` (`selfFundedPostProcedure` | `selfFundedPrepayment` | `insuredReimbursement`) added to Procedure when billingRoute=BillableParty, driving invoice wording — distinct from the Insurer route (AA billing a direct-claim insurer); (6) pre-payment gate strengthened per above; (7) a simulated `lookupNhi` (Phase 01, canned/keyed by seeded NHIs) added to the ad-hoc/manual card-creation flow, plus `validateEthnicityCode` against an explicitly-labelled NZHIS Level 4 demo subset, exercised (not just displayed) in Phase 11's FHIR bundles; (8) the time-rounding rule (round-up-per-started-interval) is now labelled a named, UI-surfaced ASSUMPTION and discovery item, not asserted as RFP fact — the RFP is silent on partial-interval rounding; (9) modifier unit values are labelled demo-plausible figures within the RFP's stated ranges, NOT an authoritative NZSA schedule — flagged as a discovery item for AA/NZSA to supply real values; (10) an explicit all-day-booking seed example added (both AM/PM Lists sharing hospital/surgeon — no special entity, per the RFP's "all day bookings simply use both lists"); (11) the contact-archiving inactivity window is a configurable setting (seeded at the RFP's illustrative 90 days, not hardcoded as a rule).
- **2026-07-22 · Third external plan review (Codex) — 13 findings: #1–3/8/9/13 adopted, #4–6/10–12 partially adopted (scoped fixes, not the literal ask), #7 rejected with a hardening.** Adopted: (#1) office route-setting made explicit — Phase 06 gains a Card/Procedure **billing-setup section** (route per the RFP's "set explicitly (by hospital advice, or by AA staff where the hospital does not specify)", insurer, billable-party/guardian, payment category, contract pick) and the mobile ad-hoc creation path captures an initial route (the anaesthetist as conduit of the hospital's advice; office corrects) — closes the gap where Phase 04 was read-only and Phase 07 review-only; (#2) **Card Copy is the RFP's additional-procedure mechanism**, not a free-standing convenience: the copy records `copiedFromCardId` and its procedures are `isAdditional` from the first (time-only; base/modifier structurally disabled) — closes the double-charging side door; (#3) **the canvas now rolls**: `advanceDays` generates new far-edge days from Permanent Lists with the seed generator (guard-tested) — seeded-once-never-rolled was a real gap vs RFP principles 1/6; (#8) **ContractHolderOrganisation** entity added; COS seeded holding an externally-held ACC contract (the RFP's "held externally instead" case); (#9) the default-contract guarantee **re-scoped to the RFP's exact wording** — hospitals + direct-billing insurers only, expired/absent Type 2/3 there *falls back* to the protected default Type 1 (new Phase 08 test); surgeon/group/organisation holders have **no mandated default**, and Phase 09's billing-failure demo now uses one of those (the old generic "expired contract" example would wrongly fail a hospital case that should fall back); (#13) three RFP open questions added to §11 as pure discovery items — hospital-route non-payment fallback, future insurer-specific rate structure, Xero duplicate-invoice-number-prevention config (also a Xero-sim callout). Partially adopted: (#4) permanent-list surgeon — already a registered §11 tension, but the seed depended on it unresolved: reading now picked, `surgeonId` (nullable) on PermanentList (RFP's "~80% defined in the Permanent List"; its principle-6 field list omits surgeon — still a discovery flag); (#5) patient-without-NHI — RFP-internal tension ("unique identifier: NHI" vs "linked to an NHI **where available**"): `hiddenInternalId` is now the Patient's invariant key, `nhi` optional, one seeded provisional "NHI pending" patient; NHI-driven behaviours apply when present; (#6) guardian identity — already a §11 tension, reading now picked: **BillableParty** is a typed identity (name/relationship/contact, own hidden internal ID — not a Patient row); Xero resolution gives each payer its own hidden-ID contact, NHI dedupe patients-only; (#10) pre-payment — **the completeCard gate + audited override stand as ruled in the 2nd review (not re-litigated**: a browser prototype cannot gate a theatre list, and the override models the office's real-world judgement**)**, but the placement point exposed a real loss: the pre-day surfacing dropped in the round-2 rewrite returns as a **"pre-payment outstanding" flag** (mobile card / admin day view / review) from invoice-raise until paid-or-overridden; (#11) Phase 09's checklist wrongly asked to verify Phase 10's payment simulation — reworded to the seeded paid state, with an explicit deferred-verification item added to Phase 10's checklist (the guard design already read paid-state, so no design change); (#12) billedAt trigger — reading unchanged (invoice generation; already a §11 entry, and the RFP itself calls the trigger "a build detail to confirm", glossing generation as submission-to-Xero), but semantics made precise: **billedAt = completion of the List's billing run**; a per-card failure or a Xero handoff failure does not restore anaesthetist visibility — the billing monitor owns both. Rejected: (#7) "incomplete Card can be moved into a SUBMITTED List" — misreading: the RFP's all-Cards-completed rule gates the DRAFT→SUBMITTED *transition*, while office edits of SUBMITTED Lists are RFP-sanctioned and the office check is explicitly "not a system gate"; `reassignCard` keeps allowing SUBMITTED targets. Hardening only: Phase 07's review FLAGS column now surfaces any not-completed Card so a moved-in incomplete Card is visible at authorisation.
- **2026-07-22 · Fourth external plan review (Codex) — 8 findings: #5/#8 adopted, #1/#6/#7 adopted as labelling only (no behaviour change), #2/#3/#4 rejected.** Adopted: (#5) **archive job widened to all individual contacts** — patients *and* billable parties (RFP asks how "Patient and Billable Party records" are deduplicated and archived; guardians share the one-time-client tail) — a consistency gap created by the 3rd review's BillableParty addition; Xero contacts tab type is now organisation/patient/billable-party; a billable-party-eligible archive test added; (#8) **concurrency added to §11's pure discovery items** (RFP open question; Data-Model §5 carried it but §11 had dropped it) with the prototype's stance stated: single-user by design, audited last-write-wins, the multi-source reality *demonstrated* via audit trail + lastModifiedBy + Phase 11's live integration updates. Labelling only — behaviour unchanged, readings now in §11 + UI copy: (#1) split-billing invoice count — genuine RFP-internal tension (grouping sections: same-counterparty procedures "billed together on a single invoice", twice; Split Billing section: "two separate invoices" in either scenario) — reading kept: group by resolved counterparty, the two-invoice outcome arises when funders differ, per the RFP's own cross-reference from the multi-invoice case to Split Billing; (#6) list-reassignment slot mechanics (free-target/absorb/regenerate, adopted 1st review) now labelled the prototype's *proposed* answer to the RFP's explicitly-open mechanism question — replaceable, stated in the Phase 06 confirm dialog; (#7) billing-monitor placement in the Admin app now labelled a picked reading of the RFP's open Admin-app-vs-separate-surface question (Phase 09 UI copy; readings count 3→4). Rejected: (#2) billedAt-before-Xero — **re-litigates 3rd-review #12 with no new evidence**; ruling stands (billedAt = billing-run completion; Xero failures are the monitor's job, and the RFP itself calls the trigger "a build detail to confirm"); (#3) pre-payment gate placement/override — **third raise of a twice-settled ruling** (2nd review made it a hard gate + audited override; 3rd review added the pre-day "pre-payment outstanding" flag); the newly-suggested "gate Start Now" variant is worse — it would block recording a clinical fact that has already happened; (#4) post-op addendum — not "silently" resolved: "post-op charges vs locked Cards (new Card/addendum)" has been a §11 labelled reading since the register was created, Phase 09 requires the label in UI copy, and the reviewer's alternative (append to the Procedure record) collides with the RFP's own hard immutability rule; RFP's "more than one billable line item over time" framing is satisfied by the linked addendum billing separately.
- **2026-07-22 · Fifth external plan review (Codex) — 11 findings: #1/#2/#4/#6/#10 adopted, #3/#5/#7/#11 partially adopted, #8/#9 rejected as repeat raises.** Adopted: (#1) **non-RVG billing-line capture added to mobile** (Phase 04): per-Procedure "add billing line" — ancillary fixed-amount lines on any card; a rate×time (hours × agreed rate) line offered only under a contract carrying the new `permitsIndividualArrangement` flag (the RFP is explicit that Method 3 "flows into the mobile app through the same capture path as any other BillingLine entry" — the capture side was genuinely missing while the validator already accepted non-RVG lines); seed gains the permitting contract + a rate×time card, exercised in Phases 04 and 08; (#2) `Contract.holderType` gains **billableParty** (individual-arrangement contracts only — the RFP: "a Billable Party, Hospital, or Insurer must hold a Contract that explicitly permits an individually-arranged structure"; B2 already asserted these contracts existed but the type couldn't represent the holder); (#4) **BillingLine gains an optional funder override + conservation rule** (line amounts reconcile to the procedure fee, validator-checked and tested) — B5(b)/Phase 08 already relied on "per-funder billing lines" no type carried; (#6) **List start/end-time override control** added to Phase 06's office editing (D2 modelled it; no phase built it; grid block resizes, audited); (#10) outstanding balances re-worded to the RFP's exact unit — **one flat row per individual ACCPAY invoice**, no rollup (our "line items" phrasing mirrored the RFP's own looser wording at its §"List visibility rules" but the balance-view section is explicit). Partially adopted: (#3) contract hierarchy — `individualAnaesthetist` scope completed with `anaesthetistId` + explicit precedence (individual beats organisational when both match; all seeds organisational, per the RFP); the "non-RVG procedure type" price-list key is NOT added — rvgBaseCode stands in for procedure type in the curated demo set, now labelled a demo simplification; (#5) reassignCard surgeon/hospital pairing — **no hard guard** (the RFP's pairing rule governs booking availability, not reassignment; a hard block would prevent legitimate moves, e.g. onto a Free surgeon-less List): the Phase 06 target picker now labels pairings and flags mismatches advisorily before confirm; (#7) pre-payment scope — genuinely new (prior rounds litigated timing/override, never amount): the pre-invoice covers the **patient-funded (BillableParty-route) procedures only**, full or deposit; a mixed card's contract-holder portion bills normally post-authorisation; balance = remainder of that portion (B7, Phase 09, tests); (#11) scale — bulk simulation of 28k invoices rejected as gold-plating (§10/P10 fences), but the Xero-sim archiving callout gains **seeded aggregate counters** (≈28k invoices/yr, ~99% one-time, active-contact count near the 10k soft limit) visibly reduced by the archive job; N4 now states scale is narrated, not simulated. Rejected: (#8) pre-payment gate placement/override — **fourth raise** of the 2nd-review ruling (reaffirmed 3rd and 4th reviews); no new evidence; (#9) billedAt-before-Xero — **third raise** of 3rd-review #12 (rejected again 4th review); the "hidden unbilled work" concern is the billing monitor's recorded responsibility.
- **2026-07-22 · Sixth external plan review (Codex; user pre-excluded the settled pre-payment/billedAt rulings) — 9 findings: #1/#2/#4/#5/#6/#8 adopted, #3/#7/#9 partially adopted, none rejected.** Adopted: (#1) **integration correlation key** — Card gains an optional `{sourceFeedId, externalAppointmentId}` ref (HL7 SCH appointment ID — the RFP sample's `1661243` — or FHIR Appointment identifier); S13/S14/S15 locate their Card by it (MSH-10 dedupes *messages*, this key correlates *appointments*); the monitor shows both keys; (#2) `validateCardForBilling` now requires an insurer that **`acceptsDirectClaims`** on the Insurer route (RFP: "only available where that Insurer accepts direct claims"), with rejection tests; (#4) Phase 07's Contracts master screen synced to the full contract model (billable-party holders, organisation-vs-individual scope + anaesthetist selector, `permitsIndividualArrangement`) — a 5th-review sync miss; (#5) Anaesthetists master screen gains registration-number ID + contact details (2nd-review type facts never propagated — RFP: "basic contact information plus registration number (ID)"); (#6) **`billingReference` field added to Procedure** (the hospital's contract/approval reference, RFP's Type-1-for-reference note) — A4's "reference completeness" check finally has a field behind it; office-editable in Phase 06's billing setup, flagged when missing in Phase 07's review, seeded with 1–2 deliberate gaps; (#8) **web-app parity extended to M6/M7** — card copy and ad-hoc/photo creation available from the web List detail via shared components (RFP: mobile statements "apply equally to the anaesthetists' web app view"), desktop layout per convention 16. Partially adopted: (#3) ACC-direct-billing prohibition — **no engine guard** (the RFP itself says who the holder claims from is "invisible to the billing engine"): instead an informational `accRelated` flag on Procedure (which W4's ACC column needed anyway — it previously had no source field) drives an **authorisation-review advisory** when such a procedure sits on the Billable Party route; one accRelated procedure seeded; (#7) automated-action audit — N3/A7 always demanded it but Phases 08/10 never carried it through: explicit audit work items added (billing run: invoice creation/billedAt/per-card outcomes; Phase 10: Xero handoff, webhook/poll, ACCPAY flips, payables run, archive job — all source=system) plus a Phase 10 checklist item reconstructing one invoice's automated trail end-to-end; binding convention 7 left unchanged (Card/Procedure-scoped) since N3 already states the global rule; (#9) production-scale proof — reseeding the demo at 85 anaesthetists rejected (the 14-cast design-day fidelity is a logged decision), but Phase 02 gains a **canvas-generator scale test**: 85 × full horizon ≈ 20k Lists, invariants intact within a stated time budget, proving P10's "structure must support 85".

- **2026-07-22 · Seventh external plan review (Codex ×2, consolidated: an 18-finding everything-pass "A" + a 23-finding per-slice pass "B", ~10 overlapping) — 19 adopted, 9 partially adopted (scoped fixes, not the literal ask), 4 rejected as repeat raises.** Adopted: (A1/B15) **BillableParty-route validator corrected** — the payer *defaults to the patient* (RFP: "billableParty (→ Patient by default)"); the typed override record is required only when someone else pays — the validator no longer demands one; (A2) **`patientPaymentCategory` validator-required** on the Billable Party route (the RFP's three categories determine recipient wording and workflow); (A3) **captured BTM inputs persist as data** — asaClass, selected modifier codes, baseUnitsSelected for range codes, overridden B/T/M with provenance (RFP principle 10: invoices reproducible against what was true — needs the inputs, not just totals); (A4/B4) **two-funder allocation gets capture UI** — office-side per-line funder editing in Phase 06's billing setup (funding arrangements are office knowledge, consistent with route-setting), one two-funder card seeded; (A6/B5) **price-override capture completed** — office gets the full typed editor (fixed/$/% + mandatory reason); mobile's legacy Adjustment/Charge fields now write the typed $/fixed overrides and demand the reason; (A7/B1) **lifecycle/source guards explicit** — submitList only from DRAFT, authoriseList only from SUBMITTED, integration-sourced writes only while the List is DRAFT (the RFP state table: SUBMITTED is office-only); (B11) an integration message targeting a SUBMITTED/AUTHORISED List's Card is refused into a **monitor manual-intervention item** — immutability and inbound updates reconciled; (A8) the mutation wrapper **stamps `lastModifiedBy/At`** in lockstep with each audit entry; (A9) availability reconciliation restatuses only **truly-Free** Lists — an empty-but-reserved List conflict-flags (RFP principle 3: status is meaningful with no Cards; "Free/empty" was a real ambiguity); (A10/B18) **shared `upsertPatient` intake dedupe** across every card-creation path (manual/photo/HL7/FHIR/PDF), NHI-matched both formats; (A12/B10) **S13 cross-List reschedule demo** — a second canned S13 moves its Card to another day/session via `reassignCard(source=integration)`; (A13/B20) **demo clock gains time-of-day** (`now()`/`advanceMinutes` + control-panel control) — Start/Finish Now needed times, not dates; (A14) Phase 07's checklist stopped demanding "matching totals" against the mockup whose simplified maths we're forbidden to inherit — anatomy matches, figures come from the real calculator; (A15) correlation-ref label corrected **SCH-1 → SCH-2** (the RFP sample's `1661243` sits in SCH-2; SCH-1 is empty); (A16/B7) **successive-partials accounting modelled** — cumulative received/authorised/disbursed per pair, per-payment idempotency key alongside InvoiceID, payables runs pay `authorised − disbursed` (tested: partial → run → partial → run, no double-pay); (A17/B16) **GST report is a transaction list** (date-ranged rows of amounts received, each with GST; totals only as footer) — the RFP asks for the list, our "summary" wording under-promised; (A18) index.html's phase-04 overview/checklist still said *validation-gated* submission — corrected to completion-gated (doc-sync miss, same class as 2nd-review #1); (B14) **default-contract invariant gains its creation half** — adding a Hospital or flipping an Insurer to direct-billing atomically creates its default Type 1 (guard-tested; deletion-protection alone couldn't honour "always holds"); (B23) **audited soft-cancel added** (`cancelCard`, reason required) — the legacy "Delete Card" had no home anywhere; cancelled Cards retained/visible, excluded from validation/billing, blocked on AUTHORISED; anaesthetist on DRAFT, office on DRAFT/SUBMITTED; **S15 reuses it — this also settles Phase 11's deferred cancellation-behaviour decision**; (B6-part) **prepayment full|split typing given a home** — `{type: full|split, depositAmount?}` on the Procedure beside its `selfFundedPrepayment` category; the Card-level "pre-payment required" flag is *derived* (B7 asserted a typed flag no type carried). Partially adopted: (A11) invalid inbound ethnicity — neither a mere warning nor message rejection: the code is **quarantined** ("pending correction", never stored — the RFP mandates NZHIS L4), the Card still books, the manual-fix flow supplies a valid code; (B8) ACCREC/ACCPAY half-failure — no half-failure simulator, but the handoff is **pair-atomic** with idempotent retry (both-or-neither, tested); (B9) integration **auto-retry demoed** — one canned transient-failure message visibly retries with an attempt count, the malformed one exhausts retries into dead-letter (the monitor's "retried" status is mechanically produced, and the RFP explicitly asks for retry logic); (B12) PDF pathway — full reschedule/cancel flows rejected (manual changes are office edits, A2), but extraction review gains **duplicate matching** ("already booked — will update, not duplicate"); (B13) HL7 translation made honest — a **minimal mapping-driven extractor** over the canned set (not per-message canned outputs, not a general parser — §10 fence stands) so editing the mapping genuinely changes reprocessing; (B17) RBAC — **view-scoping stated and demonstrated** (persona-scoped apps; availability exposes session status only; admin-only surfaces) — labelling, no architecture change; (B19) ethnicity validator verdicts now **three-way** — valid / outside-demo-subset (honestly labelled, never "invalid") / malformed; (B22) web views surface **actual List times** (Lists-table From/To incl. office overrides, week-strip labels) — no proportional-ruler redesign (convention 11 + the design mockups govern); (B21) 85-row admin day grid — rejected as a UI-shaped re-raise of 6th-review #9 (14-cast demo day is a logged decision; scale is narrated + scale-tested), adopted as a one-line **footer note** naming the production paging/virtualising answer. Rejected as repeat raises, no new evidence: (A5/B3) **ACC hard prevention** — settled 6th review #3 (the RFP itself makes ACC-relatedness "invisible to the billing engine"; the accRelated review advisory stands); (B2) **billedAt-before-Xero** — fourth raise of 3rd-review #12 (rejected again 4th and 5th reviews; the RFP calls the trigger "a build detail to confirm"); (B6-part) **pre-payment gate placement/"too late"** — fifth raise of the 2nd-review ruling (reaffirmed 3rd/4th/5th reviews); only its typing gap was new (adopted above).

- **2026-07-22 · Phase 00 build — stack & token decisions locked.** (1) **Tailwind v4** adopted (`@tailwindcss/vite` plugin, CSS-first `@theme`, no `tailwind.config.js`, no PostCSS). (2) **TS token modules are the runtime source of truth** components read (`theme/tokens.ts`, `statusColours.ts`, `motion.ts`); `global.css`'s `@theme` block **mirrors the same hexes** so Tailwind emits matching utilities — keep both in step. Most Phase-00 components style via inline styles reading the TS tokens (mirrors the mockups' own inline-style approach and keeps values 1:1); Tailwind utilities are available for later phases. (3) **React 18 pinned** (`react@^18.3.1`) — the `create-vite@9` template shipped React **19**; overridden per convention 2. (4) **Newer template than the plan anticipated, accepted as-is:** Vite **8**, TypeScript **~6.0.2**, `@vitejs/plugin-react` 6, and **oxlint** (kept the template's `lint` script alongside the required `dev`/`build`/`test`/`preview`). Node 20.19. (5) Template compiler flags kept **on**: `strict` + added `noUncheckedIndexedAccess`, plus the template's `verbatimModuleSyntax` and `erasableSyntaxOnly` — so domain code uses `import type`, and `as const` objects + string-union types instead of `enum`s. (6) **Demo clock** lives at `src/domain/demoClock.ts` exporting `DEMO_TODAY = 2026-07-21` (+ `DEMO_TODAY_LABEL`, `DEMO_CLOCK_TIME`); a pinned constant this phase, superseded by Phase 02's advanceable clock. (7) **Phone frame** 390×844, adapted from `ios-frame.jsx` into `shell/PhoneFrame.tsx` (dependency-free), floating on the `#E4E8E6` backdrop. (8) The **harness bar** (ink top bar: product name · persona · app-switcher) is prototype chrome distinct from each app's own product nav; the URL is the source of truth for the active app and the shell store mirrors it (persisted to localStorage) so a refresh returns to the same app.

- **2026-07-22 · Phase 00 post-review refinements (user feedback).** (1) **Wordmark now the real identity.** Web top-nav uses the actual logo image — the short/cropped `docs/assets/Anasthesia-logo-Short.png` (300×43) copied to `aa-prototype/src/assets/aa-logo.png`, via a shared `Logo` component at h24. (The earlier full-height `Anasthesia-logo-1.png` was swapped out for the cropped version the same session.) Admin's dark side-nav uses a shared **`Wordmark`** text component instead of the image (a crimson-on-transparent logo on dark looked wrong even on a white plaque): white serif "Anaesthesia │ Associates ᴸᵀᴰ" with a crimson divider + the crimson `ADMIN` eyebrow — matching the reference admin design and echoing the logo's structure. `Wordmark` has a `tone` prop (light = crimson text, dark = white text). (2) **Harness bar cleaned** — dropped the redundant "AA" icon and the em dash; the product name now reads `AA Booking & Billing` with a muted `PROTOTYPE` pill. (3) **No en/em dashes in app copy** — new CLAUDE.md rule (§ Copy & typography); all rendered strings cleaned (e.g. "Free to book", "Phases 03 to 04"). (4) **Playwright added as a visual-testing aid** (`@playwright/test` + Chromium; `playwright.config.ts` auto-boots the dev server; `visual/screens.spec.ts`; `npm run shots` writes gitignored PNGs to `visual/shots/`). Vitest scoped to `src/**` so it ignores the Playwright specs. Used it to eyeball all six screens this session.

- **2026-07-22 · `Wordmark` re-fit to the real logo (user feedback).** The admin side-nav wordmark's crimson divider was silently rendering at **0 width** — the empty divider span was the only flex item with a 0 min-content width, so the row's slight overflow crushed it (fix: `flex: 'none'`); that also explains the oversized word gap. Geometry now matches the logo: baseline-aligned words with tight ~0.28em gaps around the divider, the 1.5px rule dropping just below the baseline (echoing the logo's rule descending toward its tagline row), and **LTD baseline-aligned in small serif caps tucked 2px off "Associates"** (was a sans superscript at the cap line). On dark the divider lightens to `#C25B72` — 1.5px of `brand.base` crimson is near-invisible on the ink nav.

- **2026-07-22 · Time-unit partial-interval rounding = round UP per started interval — a named ASSUMPTION, not a settled rule.** The RFP defines the T1/T2 tiers (1u/15min first 2h, 1u/10min after) but is SILENT on partial intervals. Implemented as `PARTIAL_INTERVAL_ROUNDING = 'up'` in `domain/billing/timeUnits.ts` with an assumption comment; surfaced as a one-line note on the Demo Control Panel (a demo-only surface, so "nothing visual changes" in the three apps holds); Phase 04 must repeat it as a T-stepper tooltip (parked in Discovered for later). Open discovery item for AA.
- **2026-07-22 · NHI check digits use the official Health NZ algorithms** (user-approved at planning). Current AAANNNC = weighted sum mod **11** — the RFP's "Modulus 24" label appears to conflate the 24-letter alphabet with the modulus; its own example ZAA0067 validates only under mod 11 (weighted sum 191). New AAANNAX = mod **23** as labelled (example ACA31FM validates, sum 57 → check M). Flagged RFP reading / discovery item; REQUIREMENTS D8 repeats the RFP's label and carries this note. Both examples are pinned as unit tests.
- **2026-07-22 · ASA seeding values (demo-plausible, labelled):** AS1=0, AS2=1, AS3=3, AS4=4 — inside the RFP's 0 to 4 range; AS3=3 is pinned by the phase checklist's paper spot-check ((10+11+3)×30 = $720, itself a unit test). Falls under the existing "demo-plausible modifier values" discovery item (2nd review #9).
- **2026-07-22 · Type 3 second-procedure fallback (demo reading):** when a Type 3 contract has no ContractPrice matching the procedure's ordinal (most-specific-match-wins over contractId + optional rvgBaseCode/surgeonId/procedureOrdinal), the fee falls back to the BTM path — time-only if `isAdditional`. The RFP says only "various rules apply"; labelled a demo reading in `domain/billing/contracts.ts`/`fee.ts`.
- **2026-07-22 · `demoClock.ts` renamed `clock.ts`** (the phase doc's name), keeping Phase 00's `DEMO_TODAY`/`DEMO_TODAY_LABEL`/`DEMO_CLOCK_TIME` exports; 3 importers updated. Clock functions are PURE over an explicit `DemoClockState` (`todayISO` + `minutesSinceMidnight`, seeded 08:00) so Phase 02's store can own the state: `today`/`now`/`advanceMinutes` (rolls midnight)/`advanceDays`, plus seeder seams `horizonFor` (today −14d to +4 months) and `enumerateDatesISO`.
- **2026-07-22 · Modifier code table lives in `src/domain/billing/modifierCodes.ts`** (the calculator and its tests need the values now); Phase 02's seeder treats it as the ModifierCode master. Values explicitly labelled demo-plausible per the logged decision; all RFP-named groups present (PA1-5, A1-2, AS1-4, ASE, OB1-4, P1, AI1, PO1-2).
- **2026-07-23 · ZAE0311 → ZAE0310 (Phase 02 seed).** The mockup NHI ZAE0311 (David Chen) fails the mod-11 check (expected check digit 0); RFP rule beats mockup simplification (convention 17b), so the seed carries **ZAE0310**. The other three design-day NHIs (ZBC1123, ZAD2210, ZAA0067) validate as printed.
- **2026-07-23 · Slot-hashed generator randomness.** The canvas generator derives each slot's RNG as `mulberry32(hash(SEED, anaesthetistId, dateISO, session))` (xmur3-style string hash in `domain/seed/slotHash.ts`), NOT a sequential stream — generation is order-independent, so rolling the horizon forward day by day produces byte-identical Lists to seeding the whole horizon at once. This is what makes the roll-forward guard test ("far edge deep-equals a fresh generation") provable; `canvasRoll.test.ts` pins it.
- **2026-07-23 · SXAP Type 2 agreed rate = $26.50.** Southern Cross holds the SXAP Type 2 (`agreedUnitRate` 26.50): `selectContract` genuinely prefers it over the SX default Type 1 (specific-beats-default exercised) while the design-day fees still match the mockups to the cent (Tane $318.00 / Marsh $185.50 / Chen $238.50 / Ellison $344.50; 41 units, $1,086.50 — all pinned as unit tests through the real tiered calculator). Chen's "T adjusted +1 manually" story is seeded as `timeUnitsCaptured {units: 4, source: 'overridden'}` — the overridden provenance is what Phase 07's flag keys off.
- **2026-07-23 · Seeded SUBMITTED queue sits on past dates.** The mockups show end-of-day states but the clock seeds at 08:00; the two SUBMITTED lists awaiting authorisation are dated **Mon 20 Jul** (Morrison, St George's / Mr Tan, 6 completed cards + 1 cancelled) and **Fri 17 Jul** (Whitaker, Chch Public acute, 5 cards). Souter's PM submits live in later phases and becomes the queue's 3rd item (the mockup badge "3"). Souter's own design-day tableau is seeded as the mockups show it (captured times later than 08:00 accepted for demo fidelity — the presenter advances the clock).
- **2026-07-23 · Availability reconciliation, both directions.** RUNTIME `setAvailability` follows the phase doc: only a truly-Free List (Free status, no hospital/surgeon, no active Cards) restatuses; anything with booking context conflict-flags, never silently changes. The un-block direction is the symmetric picked reading: `kind: 'available'` restatuses a List back to Free only when its status is holiday/unavailable AND it carries no hospital/surgeon/Cards. At GENERATION time, availability windows take their slots cleanly (a templated booking under an approved-leave window yields a clean Holiday List, no conflict) — approved leave is already-actioned office knowledge, and the design day shows Beaumont/Ngatai as clean Holiday rows; conflicts at generation come only from hospital holidays over booked Lists.
- **2026-07-23 · Seed audit is minimal.** Seeding is initial state, not mutation: audit entries are seeded only for staged lifecycle facts (the two lists' submits, the cancelled card's cancel, completes on staged/pinned cards) — 33 entries, not one per row. Generic filler history is state-only; every runtime mutation audits via the wrapper.
- **2026-07-23 · Deterministic IDs, no UUIDs.** Seed ids are structured/sequential (`L-{anae}-{date}-{session}` slot-derived Lists, `C0001`/`P0001`/`BL0001`/`PT0001`/`A0001`); runtime allocations continue from persisted per-entity counters in store state (`allocateId` in `mutate.ts`; regenerated Lists use the `LG####` counter series since their slot id is taken by the moved List). No `Date.now()`/`Math.random()` anywhere — timestamps come from the clock state (`clockISO`, local-naive), randomness from the seeded RNG.
- **2026-07-23 · One store, versioned persistence.** A single `createAppStore()` factory (isolated non-persisted stores for tests; the persisted `useAppStore` singleton for the app) with `zustand/persist` under key `aa-demo`, `PERSIST_VERSION = 1`; a version mismatch discards persisted state so the next load reseeds — **bump the version whenever the seed shape or content changes**. Phase 00's `shellStore` migrated in as the `shell` slice ('aa-shell' key retired); guards/actions take the store `api` as their first argument.
- **2026-07-23 · Contract-holder placements (seed).** ACC-via-hospital Type 2 held by **St George's** (avoids two same-rank Type 2s at one hospital confusing later auto-selection — SX already holds SXAP); COS holds the externally-held ACC Type 2 (group holder, no default fallback); the bariatric Type 3 is held by surgeon **Mr Doyle** (ordinal-2 price row keyed to the concurrent hernia repair 49120); the `permitsIndividualArrangement` contract is **billableParty-held** (Aria Skin and Laser Clinic — exercises the 5th-review holder type) and the seeded rate×time card bills under it. Governing contracts are stored explicitly per procedure — `selectContract` is the auto-suggest path, not the validation path.
- **2026-07-23 · PermanentList type gains `hospitalId: HospitalId | null` and optional `notes`.** The recurring pre-op assessment clinic runs at AA's own rooms, not a hospital (nullable hospital), and template display labels ("Acute theatre", "Elective ortho", "Pre-op clinic / AA rooms") carry onto generated Lists as their notes. Small Phase 01 type extension, no behaviour change elsewhere.
- **2026-07-23 · Store write discipline is grep-provable.** `mutate.ts` is the ONLY module that raw-writes domain slices (audit append + `lastModifiedBy/At` stamp in one commit; procedure/line edits stamp the parent Card). `storeDiscipline` (in `mutate.test.ts`, same `import.meta.glob` pattern as domainPurity) mechanically enforces: `.setState(` only in `mutate.ts`/`clockActions.ts`, clockActions' setState writes only the clock, appStore's initializer `set()` writes only the shell slice.

## Discovered for later

Small issues/ideas noticed mid-phase that belong to a later phase — park them here instead of
scope-creeping.

- **Phase 04:** the T stepper must repeat the partial-interval rounding assumption as a tooltip
  ("rounds up per started interval; RFP silent; to confirm with AA") — the Demo Control Panel
  carries the note from Phase 01, the capture UI must too (Decisions log 2026-07-22, Phase 01).
- **Phase 06:** the Admin app's header still shows the static `DEMO_TODAY_LABEL` from Phase 00 —
  the rebuilt day view must read the live clock (`useToday()`), like the control panel and
  inspector already do.
- **Phase 05:** the design dashboard's "Who's free Wed 22 PM" names Dr Ngatai, but she is seeded
  on leave until Wed 29 (the mockups are internally inconsistent here) — the live availability
  data wins; expect the panel's names to differ from the mockup.

---

## Phase entries

Append one entry per completed session, newest last, using this template:

```
### Phase NN — <name> (YYYY-MM-DD)
**Built:** what actually shipped, file-level where useful.
**Deviations from plan:** none / list with reasons (mirror into Decisions log if binding).
**Manual test checklist:** each item ✓/✗ with notes.
**Known gaps / handoff notes:** what the next session must know.
```

### Phase 00 — Scaffold & three-app shell (2026-07-22)

**Built:**
- **Scaffold** at `aa-prototype/` (Vite `react-ts`). Deps: React 18 (pinned), react-router-dom 6, zustand 5, lucide-react, date-fns, Tailwind v4 (`@tailwindcss/vite`), Vitest + jsdom + Testing Library. Scripts `dev` / `build` (`tsc -b && vite build`) / `test` (`vitest run`) / `preview` (+ template `lint`). `tsconfig.app.json` gains `strict` + `noUncheckedIndexedAccess`.
- **Folder shape** per convention 3: `domain/` `store/` `apps/{mobile,web,admin,demo}/` `shared/` `shell/` `theme/` + `main.tsx` `App.tsx` `router.tsx`.
- **Design tokens** transcribed 1:1 from `Design Language.dc.html`: `theme/tokens.ts` (8 neutrals, brand crimson, accent teal + focus ring, semantic solid/tint/on-tint, type scale, 4pt spacing, radii, elevations e-0…e-3, scrim, phone backdrop), `theme/motion.ts` (the four named patterns + reduced-motion 80 ms), `theme/statusColours.ts` (six statuses with solid/tint/on-tint + hatched/dashed treatment recipes), `theme/global.css` (`@import "tailwindcss"` + `@theme` mirroring the hexes, Google Fonts, `.mono` tabular-nums, motion keyframes, `prefers-reduced-motion`).
- **Shared:** `StatusChip`, `StatusBlock` (honours hatched/dashed), `StatusLegend` (full + chips variants), `DemoBadge`.
- **Shell:** `shellStore` (Zustand + `persist` → localStorage `aa-shell`), `appConfig.ts` (app registry: routes, labels, personas), `AppShell` (ink harness bar: product name · persona · switcher; URL→store sync), `AppSwitcher` (dropdown, Apps / Demo groups, click-away), `PhoneFrame` (390×844, dynamic island, status bar reading the demo clock, home indicator, `#E4E8E6` backdrop).
- **Apps:** mobile (phone frame + bottom tab bar Lists·Availability·Balances·More, greeting header), web (white top-nav, crimson serif wordmark, tabs Dashboard·Lists·Availability·Accounts, + the six-status legend panel as the token demo), admin (dark side nav Day view·Review queue [badge 3]·Billing monitor·Master data·Audit, crimson inset-left active marker, KW·Office footer). Demo control panel (pinned `DEMO_TODAY`, disabled coming-controls) + Xero and Integrations stubs, all badged. Every placeholder names its filling phase.
- **Router:** `/mobile` `/web` `/admin` `/demo/control` `/demo/xero` `/demo/integrations`; index + `*` redirect to the last-selected app.
- **Smoke test:** `theme/statusColours.test.ts` (six statuses, exact hexes, treatments) — 4 assertions, green.

**Deviations from plan:** create-vite shipped a newer stack than the plan named (Vite 8, TS ~6.0.2, `@vitejs/plugin-react` 6, oxlint) — accepted as-is; React pinned back to 18. Added `StatusBlock` (not named in the plan) so the legend can show the hatched/dashed treatments alongside chips — satisfies checklist item 4. Kept the template's `lint`/oxlint. All logged in the Decisions entry above; no design deviations.

**Manual test checklist:**
1. ✓ `npm run dev` serves (HTTP 200, correct `<title>`); `npm run build` clean (tsc + vite); `npm run test` green (4 passed).
2. ✓ Switcher routes to all three apps + three demo surfaces and sets the persona (derived from the active app); choice persisted to localStorage and the index/`*` route redirects to it on refresh. Verified via a throwaway mount test (since removed) + code review of the store/persist wiring.
3. ✓ (structural) Mobile renders inside the 390×844 phone frame on the `#E4E8E6` backdrop with internal scroll; web (`min-width:1240`) and admin (`min-width:1320`) render full-width. Pixel confirmation on a 1440p screen recommended.
4. ✓ Status legend renders all six statuses; smoke test asserts the exact hexes (Private `#2E66E5`, Free `#1FA463`, +tint/on-tint) and the hatched/dashed treatment flags; `StatusBlock` draws them.
5. ☐ Side-by-side eyeball vs `Design Language.dc.html` — token values transcribed verbatim and chip/wordmark anatomy replicated, but final pixel comparison is the user's to make (I cannot see rendered output).
6. ✓ No TS errors (`tsc -b` clean) and no runtime errors mounting every route (throwaway render check); dev log clean.

**Known gaps / handoff notes:**
- No domain types / seed / real screen content yet — that is Phase 01 (model + billing) and Phase 02 (seed + store).
- Nav is presentational this phase: mobile bottom tabs, web nav tabs and admin nav items don't navigate; active states are hardcoded (Lists / Dashboard / Day view). Their sub-routes land in the app phases.
- Demo controls are disabled placeholders; the demo clock is a pinned constant (no time-of-day yet — Phase 02, plus time-of-day per 7th-review A13).
- Google Fonts load via CSS `@import` (needs internet at runtime — per the Fonts decision).
- Demo-surface persona is Kirsty (Office); revisit if a demo surface warrants a different persona.
- Template's `public/icons.svg` is unused (harmless); `favicon.svg` retained. Tailwind utilities exist but Phase-00 components mostly use inline styles reading the TS tokens — later phases may lean on utilities.
- Working tree left staged/unstaged for the user to commit (agents do not commit — CLAUDE.md).

### Phase 01 — Domain model & billing calculator (2026-07-22)

**Built:**
- **`src/domain/types.ts`** — the complete typed model: Patient (`hiddenInternalId` invariant key, `nhi` optional, ethnicity + quarantine), BillableParty (own typed identity), Anaesthetist (registration number as ID, unitValue, GST period, HPI), Hospital/Surgeon/Insurer (`acceptsDirectClaims`)/ContractHolderOrganisation, Contract (types 1/2/3, 5 holder types incl. billableParty, organisation-vs-individual scope, `permitsIndividualArrangement`, protected `isDefault`, effective dates, Type 2 detail union) + ContractPrice (explicit matching keys), List (six-status key, conflicts, overridable times), Card (correlation ref `{sourceFeedId, externalAppointmentId}` per SCH-2, audited soft-cancel record, prepayment override, `copiedFromCardId`), Procedure (route/category/prepayment detail/accRelated/billingReference/`isAdditional` + captured BTM inputs as data with seeded/overridden provenance + typed price overrides), BillingLine (chargeBasis rvg/fixed/rateTime, funder override), RvgCode (single/range base units, absorbs list), ModifierCode, PermanentList (`surgeonId` nullable), AnaesthetistAvailability, HospitalHoliday, ListStatus, AuditEntry, and the Phase 08–11 shapes typed now: Invoice/InvoiceLine/BillingCase, XeroContact/XeroAccRec/XeroAccPay (cumulative partials), PaymentIn (idempotency key)/Disbursement, IntegrationFeed (field mapping)/IntegrationMessage (MSH-10, retrying/deadLetter/manualIntervention), DemoSettings. Header pins the purity convention.
- **`src/domain/clock.ts`** (renamed from `demoClock.ts`; 3 importers + `domain/index.ts` updated) — pure state-passing clock per the Decisions entry; `INITIAL_CLOCK` at 08:00.
- **`src/domain/rng.ts`** — `mulberry32` seeded RNG (`Rng` type); Phase 02's seeder reuses it.
- **`src/domain/nhi.ts`** — shared 24-letter alphabet, weights 7..2; current format mod 11 (10 ⇒ 0, remainder 0 ⇒ never assigned), new format mod 23 (check letter); `validateNhi` (normalises, detects format by character classes, plain-language reasons) + `generateNhi(format, rng)` (regenerates on remainder 0).
- **`src/domain/nzhis.ts`** — `ETHNICITY_DEMO_SUBSET` (12 Level 4 codes across the six Level 1 groups, labelled demo subset), `validateEthnicityCode` three-way verdict (valid / outsideDemoSubset with honest copy / malformed), `lookupNhi` simulated Digital Services Hub lookup (5 canned fictional patients keyed by generator-produced NHIs, both formats — Phase 02 seed must include them: CQY9304 Sarah Mitchell, WQS3635 Hemi Walker, JKL1188 Losa Tuilagi, MYY54SL Priya Nair, RUE29KR Grace Park).
- **`src/domain/billing/`** — `money.ts` (`roundToCents`/`toCents`), `timeUnits.ts` (tiered T rule + the labelled rounding ASSUMPTION), `modifierCodes.ts` (demo-plausible master + `ASA_SEED_UNITS`), `modifierUnits.ts` (sums + refusals with reasons, absorption), `contracts.ts` (`selectContract` with individual-beats-organisational and specific-beats-default ranking + effective-date filter; `matchContractPrice` most-specific-wins), `fee.ts` (`resolveBtm` with provenance, ASA class seeding its AS code without double-count; `splitBillingUnits` time-only for additional procedures; `feeFor` → structured `FeeResult` covering Types 1/2/3, Type 3 ordinal + BTM fallback, non-RVG fixed/rate×time lines, typed overrides), `validateCardForBilling.ts` (structured field-level failures, all rules from the plan incl. cancelled-Card exclusion, direct-claims insurer check, patient-as-default-payer, prepayment typing, Method 3 gate, override reason, two-funder conservation to the cent).
- **Tests: 116 green** across 11 files — every named case from the phase plan present (tier boundaries incl. 120 ⇒ 8 / 121 ⇒ 9 / 300 ⇒ 26; P1 absorbed vs allowed; AS1–AS4; range codes; every contract type + precedence + fallback; every override type; split billing; rate×time gate both ways; conservation pass/fail; all validation shapes; NHI examples ZAA0067/ACA31FM + wrong-check + malformed + generator round-trips; ethnicity three-way; lookup hit/miss; clock rollover/horizon 138 days; paper spot-check $720). `domainPurity.test.ts` mechanically enforces no React/DOM imports in `src/domain/`; `statusKeyParity.test.ts` asserts domain `ListStatusKey` ≡ theme `StatusKey` at compile time and runtime.
- **Demo Control Panel** gains the one-line rounding-assumption note (demo-only surface — the three apps are visually unchanged).

**Deviations from plan:** one, mechanical — the purity test reads domain sources via Vite's `import.meta.glob(?raw)` instead of Node `fs`: the template tsconfig's `types` array excludes `@types/node`, so `node:fs` fails `tsc -b`; the glob approach needs no config change and scans the same tree. Everything else landed per plan.

**Manual test checklist:**
1. ✓ `npx vitest run` green (116 tests, 11 files) and the suite list reviewed against every named case (verbose run inspected).
2. ✓ `npm run build` green (`tsc -b` + vite); purity test proves no React/DOM imports in `src/domain/` (and no `.tsx` files there).
3. ✓ Paper spot-check by hand: 2h30m ⇒ T = 8 + ceil(30/10) = 11; (10 + 11 + 3) × 30 = **$720** — matches the pinned test (`fee.test.ts` "PAPER SPOT-CHECK").
4. ✓ PROGRESS.md updated: status row, six Decisions entries, Discovered-for-later Phase 04 tooltip item, this entry.

**Known gaps / handoff notes:**
- Phase 02 owns: the Zustand store (which holds `DemoClockState` and calls the pure clock functions), the seeded canvas (use `horizonFor`/`enumerateDatesISO` + `mulberry32`), seeding the 5 canned `lookupNhi` patients, and treating `billing/modifierCodes.ts` as the ModifierCode master.
- `validateCardForBilling` expects procedures in Card order (ordinal = index + 1) and ALL stored billing lines in its context; conservation semantics: once any line carries a `funderOverride`, the procedure's stored lines are the explicit allocation of the WHOLE fee.
- `feeFor` takes only non-RVG lines as inputs (`nonRvgLines`) — rvg-basis stored lines are Phase 08 outputs/office allocations, never fee inputs.
- The status-bar clock display (`DEMO_CLOCK_TIME`) is still a pinned string; Phase 02 wires it to the clock state.
- Working tree left for the user to review and commit (agents do not commit — CLAUDE.md).

### Phase 02 — Seed data & store (2026-07-23)

**Built:**
- **`src/domain/seed/`** (pure — domainPurity covers it automatically): `slotHash.ts` (xmur3-style hash + `slotRng`), `cast.ts` (the design's 14 anaesthetists with registration-number ids, distinct unit values $26 to $42, Souter $26.50, Whitaker the one non-monthly GST period, fictional HPI ids; 10 surgeons incl. Nand; 5 hospitals; nib/`AIA Health` insurers; COS organisation; the ListStatus master), `rvgCodes.ts` (34 codes across sites, 4 range codes, 6 absorbing P1, mockup-pinned bases incl. 47516 = 7; labelled demo values), `contracts.ts` (6 protected defaults, SXAP $26.50 Type 2, Health NZ Type 2, STG ACC Type 2, Doyle bariatric Type 3 + ordinal-2 price row, COS ACC, billableParty-held `permitsIndividualArrangement`), `permanentLists.ts` (48 weekly templates; Souter's reproduce her design week), `availabilityAndHolidays.ts` (leave windows incl. Souter Fri 24 to Sun 26, Beaumont "back Mon 27", Ngatai "back Wed 29", Ngata/Delaney unavailability, two future windows; Labour Day + Canterbury Anniversary per hospital), `canvas.ts` (**THE shared generator**: 2 Lists per anaesthetist per day, availability → template → slot-RNG fill, hospital-holiday conflict flags), `patients.ts` (150 patients: 4 design-day + 5 canned lookup + scenario + provisional no-NHI + 132 generated, both NHI formats, all ethnicity-coded; 2 BillableParties), `cards.ts` (146 cards/148 procedures/3 lines: the design-day tableau with the exact mockup captured times and the four fees exact to the cent, the two SUBMITTED lists, and every scenario state), `index.ts` (`buildSeed()`, `SEED = 20260721`, design fixups painting Tue 21 / Souter's week / Thu 23, **`SEED_MARKERS`** — 23 markers keyed by scenario).
- **`src/store/`**: `appStore.ts` (one `createAppStore()` factory + persisted `useAppStore` singleton, versioned key `aa-demo` v1, typed empty billing/xero/integrations scaffolds, shell slice migrated in; `shellStore.ts` deleted), `mutate.ts` (the audit wrapper: `Actor`, `Outcome` refusals-as-data, `clockISO`, `allocateId`, multi-meta commits, parent-Card stamping, `resetDomainState`), `lifecycle.ts` (`completeCard` with the ordered `completionBlockers` list — Phase 09's prepayment gate appends there; completion-gated `submitList`; strictly-ordered `authoriseList` emitting `listAuthorised`; audited soft-`cancelCard`; `editCard`/`editProcedure` with the full role/source/state matrix; `reassignList` free-target/absorb/regenerate with canvas invariant; Card-level `reassignCard`; `setAvailability` master-write + reconcile), `intake.ts` (`upsertPatient` NHI dedupe both formats, provisional records, ethnicity quarantine), `mastersActions.ts` (`createHospital`/`setInsurerDirectClaims` atomically create the protected default Type 1), `clockActions.ts` (`advanceClockMinutes`/`advanceClockDays` — midnight rolls the canvas via the shared generator, one `canvas.rollForward` audit entry per day, source demo; `resetDemo`), `events.ts` (`listAuthorised`/`dayAdvanced` seams), `selectors.ts` (plain read helpers + `billingContextForCard` + primitive hooks).
- **UI:** Demo Control Panel live (clock display + advance +15 min/+1 h/next day/+7 days, two-step reset confirm; Phase 11/12 placeholders and the rounding note stay); PhoneFrame status bar reads the store clock (`DEMO_CLOCK_TIME` removed from `clock.ts`); **`/demo/data` inspector** (new `demo-data` AppId + route + switcher entry): entity counts, today's 14×AM/PM table with status chips and an explicit canvas-invariant banner, the SEED_MARKERS scenario finder with entity JSON, pick-a-card audit trails, lifecycle-state filters, and a guard console (persona × action × target → outcome, refusal messages rendered).
- **Tests: 188 green** across 16 files (52 new): seed determinism/canvas invariant/every status colour/80% band (0.7 to 0.9)/marker resolution/NHI validity both formats/canned patients/design-day fees $318.00-$185.50-$238.50-$344.50 + 41u $1,086.50/bariatric $2,800+$950/rate×time $1,440/two-funder conservation/SUBMITTED lists complete+valid/exactly-2 missing billingReferences; the full guard matrix from the phase doc incl. reassign mechanics + setAvailability both directions; wrapper stamping in lockstep + the `storeDiscipline` source scan; intake dedupe/provisional/quarantine + master-data invariants; clock roll-forward deep-match + midnight rollover + reset determinism + the **85-anaesthetist scale test** (23,460 Lists, well under the 2 s budget).

**Deviations from plan:** (1) `PermanentList` gained `hospitalId: … | null` + optional `notes` (pre-op clinic isn't hospital-hosted; template labels carry to Lists) — logged as a decision. (2) The generator applies availability windows cleanly at generation time (no conflict flags for approved leave over templates; the plan's conflict line is the runtime `setAvailability` behaviour) — folded into the availability decision, keeps Beaumont/Ngatai as the design's clean Holiday rows. (3) The bariatric ordinal-2 price row is keyed to the concurrent hernia (49120), not a second bariatric code — clinically plausible and it still exercises most-specific-match. (4) The guard console covers complete/cancel/edit/submit/authorise; reassign and setAvailability are exercised by tests rather than the console. Everything else landed per plan.

**Manual test checklist:**
1. ✓ `npx vitest run` green (188 tests, 16 files); `npm run build` green (tsc + vite); `npm run lint` clean.
2. ✓ Inspector: canvas-invariant banner confirms 2 Lists per anaesthetist; every status colour visible in the day table and asserted in tests; all 23 seeded scenarios one click away in the marker finder (screenshot-verified via Playwright).
3. ✓ Reset → identical data: unit test proves domain-state deep-equality across resets after mutations; browser walk confirmed reset returns to Tue 21 Jul 08:00 and state survives a refresh (persisted store rehydrates).
4. ✓ Advance-day moves `today()` (browser: control panel + inspector both showed Wed 22 Jul after Next day, surviving refresh); far-edge days exist with 2 Lists per anaesthetist and deep-match a fresh generation (unit-tested).
5. ✓ Forbidden transitions in the guard console produce the refusal messages (browser walk: integration persona authorising a SUBMITTED list → "Refused (officeOnly)…"; full matrix unit-tested).

**Known gaps / handoff notes:**
- **`SEED_MARKERS`** (`domain/seed/index.ts`) is the scenario registry: `Record<key, {label, entityType, entityId, detail}>` — tests and the inspector resolve entities through it; later phases should too rather than hardcoding ids.
- **Events seam:** subscribe via `onAppEvent` (`store/events.ts`) — `listAuthorised` for Phase 08's billing run, `dayAdvanced` for Phase 10's poll/archive jobs.
- **Phase 09 extension point:** `completionBlockers()` in `lifecycle.ts` returns an ordered list; the unpaid-prepayment gate appends a second blocker there (comment marks the spot). `completeCard` refusals carry all blockers in `details`.
- **Persistence rule:** bump `PERSIST_VERSION` (appStore.ts) whenever seed content/shape changes, or returning visitors keep stale localStorage data.
- **Guard calling convention:** every guard/action takes the store `api` first (`useAppStore` singleton from UI; `createAppStore()` isolates tests). Anaesthetist actors carry `anaesthetistId` for own-list checks. Refusals are `Outcome` data — render `message` verbatim, branch on `code`.
- Lists moved/regenerated by `reassignList` no longer have slot-encoded ids — always resolve slots via `listForSlot` (it falls back to a scan), never by parsing ids.
- Cards seeded on the design day carry captured times later than the 08:00 clock (mockup end-of-day fidelity, per the logged decision) — Phase 03/04 screens should read state, not compare against "now".
- Working tree left for the user to review and commit (agents do not commit — CLAUDE.md).
